apiVersion: v1
kind: ConfigMap
metadata:
  name: phpmyadmin
  namespace: default
data:
  uploads.ini: |
    upload_max_filesize = 100M
    post_max_size = 100M
    max_execution_time = 300
    max_input_time = 300
    memory_limit = 256M