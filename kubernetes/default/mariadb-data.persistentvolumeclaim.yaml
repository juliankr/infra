apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: storage-mariadb-0
  namespace: default
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: ""
  resources:
    requests:
      storage: 50Gi
  volumeName: mariadb-data
